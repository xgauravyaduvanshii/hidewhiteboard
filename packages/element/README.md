# @hidewhiteboard/element

## Install

```bash
npm install @hidewhiteboard/element
```

If you prefer Yarn over npm, use this command to install the hidewhiteboard utils package:

```bash
yarn add @hidewhiteboard/element
```

With PNPM, similarly install the package with this command:

```bash
pnpm add @hidewhiteboard/element
```
