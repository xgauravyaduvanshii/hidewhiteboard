# initialData

<pre>
&#123; elements?: <a href="https://github.com/xgauravyaduvanshii/hidewhiteboard/blob/master/packages/hidewhiteboard/element/types.ts#L114">hidewhiteboardElement[]</a>, appState?: <a href="https://github.com/xgauravyaduvanshii/hidewhiteboard/blob/master/packages/hidewhiteboard/types.ts#L95">AppState</a> &#125;
</pre>

This helps to load hidewhiteboard with `initialData`. It must be an object or a [promise](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/Promise) which resolves to an object containing the below optional fields.

| Name | Type | Description |
| --- | --- | --- |
| `elements` | [hidewhiteboardElement[]](https://github.com/xgauravyaduvanshii/hidewhiteboard/blob/master/packages/hidewhiteboard/element/types.ts#L114) | The `elements` with which `hidewhiteboard` should be mounted. |
| `appState` | [AppState](https://github.com/xgauravyaduvanshii/hidewhiteboard/blob/master/packages/hidewhiteboard/types.ts#L95) | The `AppState` with which `hidewhiteboard` should be mounted. |
| `scrollToContent` | `boolean` | This attribute indicates whether to `scroll` to the nearest element to center once `hidewhiteboard` is mounted. By default, it will not scroll the nearest element to the center. Make sure you pass `initialData.appState.scrollX` and `initialData.appState.scrollY` when `scrollToContent` is false so that scroll positions are retained |
| `libraryItems` | [LibraryItems](https://github.com/xgauravyaduvanshii/hidewhiteboard/blob/master/packages/hidewhiteboard/types.ts#L247) &#124; Promise&lt;[LibraryItems](https://github.com/xgauravyaduvanshii/hidewhiteboard/blob/master/packages/hidewhiteboard/types.ts#L200)&gt; | This library items with which `hidewhiteboard` should be mounted. |
| `files` | [BinaryFiles](https://github.com/xgauravyaduvanshii/hidewhiteboard/blob/master/packages/hidewhiteboard/types.ts#L82) | The `files` added to the scene. |

You might want to use this when you want to load hidewhiteboard with some initial elements and app state.

```jsx live
function App() {
  return (
    <div style={{ height: "500px" }}>
      <hidewhiteboard
        initialData={{
          elements: [
            {
              type: "rectangle",
              version: 141,
              versionNonce: 361174001,
              isDeleted: false,
              id: "oDVXy8D6rom3H1-LLH2-f",
              fillStyle: "hachure",
              strokeWidth: 1,
              strokeStyle: "solid",
              roughness: 1,
              opacity: 100,
              angle: 0,
              x: 100.50390625,
              y: 93.67578125,
              strokeColor: "#000000",
              backgroundColor: "transparent",
              width: 186.47265625,
              height: 141.9765625,
              seed: 1968410350,
              groupIds: [],
            },
          ],
          appState: { zenModeEnabled: true, viewBackgroundColor: "#a5d8ff" },
          scrollToContent: true
        }}
      />
    </div>
  );
}
```
